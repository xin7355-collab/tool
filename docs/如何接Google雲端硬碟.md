# 怎麼把 Google 雲端硬碟資料夾接進來

接好之後：**把檔案丟進那個資料夾就好**，錄音、影片、PDF、照片、Google 文件都會
自動收進逐字稿抽取台，該轉錄的轉錄、該讀文字的讀文字，跟你自己上傳一模一樣。

手機、電腦、別人分享給你的，只要進得了那個資料夾就進得來。

## 為什麼是「機器人帳號」而不是叫你登入 Google

一般的 Google 登入（OAuth）在個人專案上有個很煩的限制：同意畫面停在「測試」狀態時，
**授權每 7 天就過期**，等於每週都要重新登入一次。這種每週都會壞掉的東西不能當基礎建設。

服務帳號是一個機器人 Google 帳號，金鑰不會過期。你只要把資料夾**分享**給它的信箱，
它就讀得到——**資料夾維持私人**，不用改成「知道連結的人都能看」。

它拿到的是**唯讀**權限，而且只有你分享的那幾個資料夾看得到。

## 步驟（大約 10 分鐘，只做一次）

### 1. 建一個 Google Cloud 專案

1. 開 [console.cloud.google.com](https://console.cloud.google.com/)（用哪個 Google 帳號都可以）
2. 左上角專案選單 → **新增專案** → 取個名字，例如 `transcript-deck` → **建立**

免費。這個專案只是用來裝那個機器人帳號，不會產生費用。

### 2. 開啟 Drive API，建立服務帳號

1. 搜尋列打 **Google Drive API** → 進去按 **啟用**
2. 左邊選單 **IAM 與管理 → 服務帳號** → **建立服務帳號**
   - 名稱隨便取，例如 `deck-reader`
   - 中間那兩個「授予角色」「授予使用者存取權」**都跳過**（按繼續、完成就好）
     —— 它不需要專案裡的任何權限，只需要你等一下分享給它的資料夾
3. 建好之後點進那個服務帳號 → **金鑰** 分頁 → **新增金鑰 → 建立新的金鑰 → JSON** → 建立

瀏覽器會下載一個 `.json` 檔。**這個檔就是密碼，不要外流。**

順便把那個服務帳號的信箱複製起來，長得像：

```
deck-reader@transcript-deck-123456.iam.gserviceaccount.com
```

### 3. 把 JSON 貼進 GitHub

1. 開 https://github.com/xin7355-collab/tool/settings/secrets/actions
2. **New repository secret**
3. Name：`GDRIVE_KEY`
4. Secret：**把那個 .json 檔的內容整份貼進去**（從 `{` 到 `}`，含大括號）
5. 存檔

> Secret 是加密的，日誌裡只會顯示 `***`，別人的 PR 也讀不到。

### 4. 把資料夾分享給機器人

在 Google 雲端硬碟裡，對著你要收的資料夾按右鍵 → **共用** →
貼上第 2 步那個服務帳號信箱 → 權限選 **檢視者** → 傳送。

（它不是真人信箱，寄不到通知信，Google 可能會提醒你「這個地址收不到郵件」，正常，照送。）

### 5. 在網站上加入資料夾

1. 打開網站 → **📡 追蹤** 分頁 → 找到「雲端硬碟資料夾」
2. 貼上資料夾網址（在雲端硬碟裡打開那個資料夾，網址列直接複製）
3. 按 **☁️ 加入資料夾**

網址長這樣，貼整串就行：

```
https://drive.google.com/drive/folders/1AbCdEfGhIjKlMnOpQrSt
```

加完會**馬上去看一次**，資料夾名字和收到的檔案一兩分鐘後會出現在卡片上。

## 之後就不用管了

- **每小時**檢查一次；剛加入資料夾時會立刻檢查一次。
- 每個檔案**只收一次**（記在 `.gdrive-state.json`），你把檔案留在雲端硬碟不會被重複處理。
- 一次最多收 **10 個**，剩下的下一輪繼續——避免一口氣把幾十個大檔塞進來。
- 收進來之後檔案就交給既有的產線，**原本的雲端硬碟檔案不會被動到、不會被刪。**

## 哪些收得到

| 你丟進去的 | 怎麼處理 |
|---|---|
| 錄音、影片（m4a/mp3/mp4/webm…） | 語音辨識成逐字稿，單檔 45MB 以內 |
| PDF | 有文字層直接抽；掃描件走辨識。單檔 20MB 以內 |
| 照片、掃描圖（jpg/png/HEIC…） | AI 視覺辨識文字 |
| 純文字（txt/md/csv） | 直接讀 |
| **Google 文件** | 匯出純文字再讀 |
| **Google 試算表、簡報** | 匯出 PDF 再讀（有文字層，零錯字） |
| Google 表單、地圖、捷徑 | 略過（沒有內容可讀） |
| 壓縮檔、程式檔之類 | 略過 |

略過的和超過大小的會列在卡片上，不會安靜消失。

## 出問題時

卡片上會直接寫原因，常見的兩種：

| 看到什麼 | 怎麼辦 |
|---|---|
| `這個資料夾沒有分享給 deck-reader@…` | 回到第 4 步，確認分享的信箱一字不差 |
| 卡片上寫「還沒設定服務帳號」 | `GDRIVE_KEY` 這個 Secret 還沒填，回到第 3 步 |

想馬上看一次不用等整點：Actions → **Google 雲端硬碟同步** → Run workflow。

## 想收手的時候

- 網站上按資料夾旁邊的 **✕ 移除**（已經收進來的逐字稿不會被刪）。
- 要徹底斷開：在雲端硬碟把分享取消，或到 GitHub 把 `GDRIVE_KEY` 刪掉。
- 那個 Google Cloud 專案留著不用錢，也可以直接刪掉整個專案。
