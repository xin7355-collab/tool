# 台語逐字稿怎麼開起來

一次性設定，設完就不用再管。**全程可以在手機上做完。**

---

## 為什麼台語要另外設定

原本的辨識引擎是 Whisper（Groq），它支援 99 種語言，**裡面沒有台語（閩南語）**。

重點在於：餵台語進去它**不會報錯**。它會吐出一段文法通順、讀起來很像話、
但整句都不是講者原意的國語。逐字稿看起來完全正常，只有把音檔重聽一遍的人
才會知道是錯的——這是所有失敗方式裡最難發現、也最害人的一種。

所以台語走的是另一條路：Google 的 Gemini。它的音訊輸入本來就吃得下台灣口音
與台語／國語夾雜，而且同一次呼叫就能要它「逐字寫成台文」＋「翻成華語」。

**沒設定這把金鑰的話，台語檔案會直接失敗並寫明原因，不會偷偷用 Whisper 頂替。**
國語完全不受影響，照原本的走。

---

## 「我可以不申請嗎？」

可以。**不申請的話就是沒有台語，其他完全不受影響**——國語逐字稿、文件辨識、
追蹤頻道、2GB 上傳全部照舊。

如果你不小心傳了台語檔案又沒設金鑰：那個檔案會停在「等你設定台語金鑰」，
**不會**變成一堆失敗信（系統分得出「還沒設定」和「壞掉」的差別）。
檔案留著，你之後設好就自己會跑；不想做台語的話重傳一次、語言選「國語」就好。

### 不用金鑰的替代方案：有，但需要一台有顯卡的電腦

數位發展部數位產業署開源了
[Taiwan Tongues ASR CE](https://github.com/adi-gov-tw/Taiwan-Tongues-ASR-CE)，
支援國語、台語、客語、英語，MIT 授權，完全免費、不用任何金鑰。
模型是 CTranslate2 格式，跟這個專案本來就有的本機 Whisper 同一種格式，
接起來程式改不多。

問題在機器。它自己列的需求：

| 跑法 | 需求 | 5 分鐘音檔 |
| --- | --- | --- |
| CPU | **16 核以上、32GB RAM** | 約 7 分鐘 |
| GPU | RTX 3080／4070 Ti 級，10GB 顯存 | 約 30 秒 |

GitHub Actions 的免費機器是 **4 核、16GB RAM**，連它 CPU 模式的最低要求都不到
（核心數四分之一、記憶體一半）。照這個比例推，一支三小時的節目在那台機器上
要跑十幾個鐘頭，而工作流上限是 5 小時——等於永遠跑不完。

而且它**只能自架**，沒有公開的線上 API。手機更不可能。

**所以：如果你哪天有一台有獨立顯卡的電腦（Windows 或 Linux，RTX 3080 等級以上），
跟我說，我可以把它接成第二個引擎，那樣就完全不用金鑰也不用送資料給 Google。**
在那之前，手機這條路上 Gemini 是唯一可行的。

### 三個選項擺在一起

| 選項 | 要花什麼 | 台語能不能做 |
| --- | --- | --- |
| 什麼都不做 | — | ❌ |
| Gemini 免費金鑰 | 2 分鐘，不用信用卡，音檔會送到 Google | ✅ |
| 數位部開源模型 | 一台有 RTX 3080 級顯卡的電腦 | ✅（資料不出門） |

---

## 第一步：拿一把 Gemini 金鑰（免費）

1. 手機 Safari 開 <https://aistudio.google.com/apikey>
2. 用 Google 帳號登入
3. 按 **Create API key** → 選一個專案（沒有就讓它自動建）
4. 複製那串 `AIza…` 開頭的金鑰

> 免費額度有每日／每分鐘的上限。一支三小時的節目會切成 18 段、送 18 次，
> 所以一天大概能跑幾支而已。撞到額度時工作流會自己等一下再試，
> 真的用完就留到下一輪（每 2 小時自動重跑），不用重傳。

---

## 第二步：把金鑰存進 GitHub

這把金鑰是**伺服器**在用的，不能存在手機上，所以要放進 repo 的 Secrets。

1. 手機 Safari 開
   <https://github.com/xin7355-collab/tool/settings/secrets/actions>
2. 按 **New repository secret**
3. Name 填：

   ```
   GEMINI_API_KEY
   ```

4. Secret 貼上剛剛複製的 `AIza…`
5. 按 **Add secret**

完成。畫面上會看到 `GEMINI_API_KEY`，但看不到內容——那是正常的，
GitHub 存進去之後連你自己都讀不回來。

---

## 第三步：用它

逐字稿 App →「⬆️ 上傳音檔」→ 選檔案 → **語言** 那一欄選：

| 選項 | 什麼時候用 |
| --- | --- |
| 國語（預設，最快） | 一般國語節目。走原本的 Whisper，不花 Gemini 額度 |
| 台語／台語國語夾雜 | 確定是台語的。整支走 Gemini |
| 自動判斷 | 不確定。先聽開頭 90 秒判斷，再決定走哪條 |

「自動判斷」會跳過開頭 30 秒才開始聽——片頭音樂和罐頭開場白不能代表整支的語言。

---

## 會拿到什麼

一支台語影片會產出**四個檔案**（國語影片還是照舊三個）：

| 檔案 | 內容 |
| --- | --- |
| `….txt` / `….md` | **華語版**。摘要、分類、搜尋都讀這一版 |
| `….nan.txt` | **台文版**。講者實際講出來的台語，用教育部推薦用字 |
| `….tw.md` | **對照版**。一段台文、一段華語，上下排好 |
| `….nan.srt` | 台文字幕，配影片播放用 |

下載清單上會多出「台文」和「台華對照」兩顆鈕，**只有台語稿才會出現**。

### 為什麼華語版是「主檔」

整條產線下游——摘要、自動分類、網站搜尋、分析師照妖鏡——讀的都是 `.txt`。
台文放進去會讓它們全部讀到看不懂的字。台文獨立成一個檔案，反而更好找。

---

## 台語的字有四種寫法

這是台語跟國語最不一樣的地方：**同一句話有四種合法的寫法**，不是誰對誰錯。

| 寫法 | 長什麼樣 | 這套系統怎麼處理 |
| --- | --- | --- |
| 全漢（HAN） | 我感覺猶會使看懸 | **`.nan.txt` 用這套**，教育部推薦用字 |
| 台羅（Tâi-lô） | guá kám-kak iáu ē-sái khuànn kuâinn | `.tw.md` 的「羅馬字」那行，**預設** |
| 白話字（POJ） | goá kám-kak iáu ē-sái khoàⁿ koâiⁿ | 教會傳下來的那套，可以切換 |
| 漢羅（Hanlo） | 漢字夾羅馬字 | 目前沒產出 |

台羅和白話字**唸起來一模一樣**，只是拼法不同（ts↔ch、ua↔oa、ing↔eng、nn↔ⁿ）。
模型常常這一段回台羅、下一段回白話字，混在同一份檔案裡看起來就像拼錯字，
所以系統會自己統一成同一套。想改成白話字的話，到
<https://github.com/xin7355-collab/tool/settings/variables/actions>
新增一個 Variable（不是 Secret）`TAIGI_ROMAN`，值填 `poj`。

## 已知的限制（先講清楚）

- **羅馬字常常是空的。** 我要求模型沒把握就留白——錯的拼音比沒有拼音更難用。
  所以 `.tw.md` 裡羅馬字可能整片空著，這是刻意的。
- **切在 10 分鐘邊界上的那一句會斷掉。** 一支三小時的節目有 18 段，
  所以大約有十幾句會斷在半路。換來的是整體快五倍以上（各段是平行送的）。
- **台文用字本來就沒有唯一答案。** 教育部有推薦用字，但民間寫法很多。
  看到跟你習慣不一樣的寫法，不一定是錯的。
- **第一次跑完請對一段音檔抽查。** 這是新的引擎，我沒有你那批影片的實測數字。
  抽查幾段、告訴我哪裡不對，我再調 prompt。

---

## 大檔案（1GB 以上的影片）

上傳上限已經從 45MB 提高到 **2GB**（GitHub Release 附件的單檔上限），
影片可以直接傳，系統會自己抽音軌。

要注意的兩件事：

1. **接 Wi-Fi。** 超過 200MB 用 4G 傳很容易中途斷掉。
2. **上傳中不要切走這一頁。** iPhone 一切到別的 App 就會把上傳掐掉。

上傳時會顯示百分比和已傳的 MB 數。中途斷掉的話重按一次即可——
沒傳完的東西不會留在系統裡。

超過 2GB 的檔案目前傳不上去。真的遇到的話跟我說，
可以做成「切成幾塊分別上傳、伺服器再接回來」。

---

## 出問題時

| 現象 | 原因 |
| --- | --- |
| 進度條顯示「等你設定台語金鑰」 | 第二步沒做完，或名字打錯（要一字不差）。檔案留著，不會變失敗信 |
| 「問不到 Gemini 有哪些模型」 | 金鑰貼錯或已失效，回第一步重拿一把 |
| 一直卡在某幾段失敗 | 多半是撞到免費額度。附件還留著，每 2 小時自動重試 |
| 台文看起來像國語 | 那一段講者本來就在講國語（`lang=cmn` 會原樣保留） |

執行記錄在
<https://github.com/xin7355-collab/tool/actions/workflows/audio.yml>，
點進最近一次就能看到每一段的訊息。
