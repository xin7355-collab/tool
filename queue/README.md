# queue/ 收件匣

前端「一鍵帶入 GitHub 提交」會在這個資料夾建立 `<時間戳>.txt`，
每個檔一行一個 YouTube 網址。

產線（`.github/workflows/pipeline.yml`）會讀取這裡所有 `.txt`，
連同根目錄的 `queue.txt` 一起抽逐字稿；**已抽過的影片會自動略過**，
所以重複投遞不會重抽、也不會產生重複檔案。

這裡的 `.txt` 只是投遞紀錄，隨時可刪，不影響已產出的逐字稿。
